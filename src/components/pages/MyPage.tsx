import { memo, VFC } from "react";
import { Footer } from "../organisms/layouts/Footer";

export const MyPage: VFC = memo(() => {
    return (
        <div>
            <h1>MyPageページです</h1>
        </div>
    );
});