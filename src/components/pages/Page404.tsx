import { Heading } from "@chakra-ui/react";
import { memo, VFC } from "react";

export const Page404: VFC = memo(() => {
    return (
        <Heading mt={5} ml={5} color="gray.400"></Heading>
    );
});